spring:
  application:
    name: exchange-service
  cloud:  # nacos地址
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
  profiles:
    active: dev
# 拉取的配置文件的dataID =  exchange-service-dev.yaml
